{
  "permissions": {
    "allow": [
      "mcp__claude_ai_Figma__get_screenshot",
      "mcp__claude_ai_Figma__get_metadata"
    ],
    "deny": [
      "Read(./photos/**)",
      "Read(./Images/**)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys,json; d=json.load(sys.stdin); p=d.get('tool_input',{}).get('file_path',''); o=d.get('tool_input',{}).get('offset'); (sys.stderr.write('СТОП: index.html нельзя читать целиком (~2200 строк раздуют контекст). Сначала grep -n по якорю, потом Read с offset+limit.\\n'), sys.exit(2)) if 'index.html' in p and o is None else None\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo '→ /analyze — запусти если была значимая задача'"
          }
        ]
      }
    ]
  }
}
